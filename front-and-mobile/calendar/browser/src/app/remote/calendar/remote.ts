import { createWidget } from '@abanking/feature-widget-builder-common';
import { LayoutCalendarPageWeb } from './pages/layout-calendar/layout-calendar.page.web';

export default createWidget({
    component: LayoutCalendarPageWeb,
    name: 'calendar',
});
