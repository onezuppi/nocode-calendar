import { createWidget } from '@abanking/feature-widget-builder-common';
import { LayoutCalendarPageMobile } from './layout-calendar/layout-calendar.page.mobile';

export default createWidget({
    component: LayoutCalendarPageMobile,
    name: 'calendar',
});
